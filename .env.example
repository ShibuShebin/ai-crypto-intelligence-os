# Anthropic API key — used by the orchestrator's decision-making LLM
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: only needed if you later add authenticated Binance endpoints
# (account data, execution). Not required for public market/derivatives data.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Optional: news/sentiment source, add in Day 3
NEWS_API_KEY=
